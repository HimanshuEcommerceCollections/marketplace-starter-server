# Prisma migration history lives here. Generated by `npm run prisma:migrate`
# once DATABASE_URL points at a reachable database. Commit the generated
# migration folders so every environment applies the same schema history.
