# The project path contains "&" (Elevate Health & Wellness), which cmd.exe — npm's
# default script shell on Windows — parses as a command separator and breaks
# `npm run` scripts. PowerShell treats the path as a plain string, so scripts work.
script-shell = powershell.exe
