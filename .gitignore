# dependencies
node_modules/

# npm config — never commit; script-shell=powershell is a local-Windows-only
# workaround for the "&" in the project path and breaks Linux CI/deploys (Render)
.npmrc

# build output
dist/

# env — never commit secrets
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# runtime category-asset registry (generated by the assets API)
data/

# os / editor
.DS_Store
Thumbs.db
